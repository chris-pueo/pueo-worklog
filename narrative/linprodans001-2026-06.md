# linprodans001 — 2026-06 worklog narrative

## 2026-06-16

Technology_MGMT 3hr: Stood up the deploy pipeline design + artifacts for the developer's Nexus / Pueo Staffing app (github.com/PueoBusinessSolutions/nexus) — CHG-2026-0046, ADR-0018, BL-083/084/085. Pulled + understood the app (Vue3+Vite SPA / Express+TS API + Knex/MariaDB, 18 migrations, existing GH-hosted ci.yml gate). Decisions w/ operator: self-hosted registry:2 on the swarm; auto-deploy on push→main to a shared staging at nexus.labs.pueo.com (per-PR post-1.0; nexus.pueo.com blue/green later); dedicated CI VM linprodci001 (ADR-0011 template) running the self-hosted GH runner; dev-bypass auth on the internal staging (real Entra SSO = pre-prod gate). Authored (build-validated locally, npm ci+build clean both tiers, 18 .js migrations emitted, /api baked into SPA): app-repo PR artifacts (server/client multi-stage Dockerfiles, entrypoint w/ wait-for-db→compiled-migrate→start + DB_PASSWORD_FILE secret, client nginx SPA+/api proxy, .dockerignores, deploy.yml self-hosted build→push→deploy, + 2 flagged knexfile changes: compiled-JS prod migrations + DB_SSL toggle); infra-repo (docker-swarm/registry/ + docker-swarm/nexus/ stacks+deploy.sh+READMEs, ingress server blocks for nexus/mail.nexus/registry rolled 0606→0616). All YAML+shell syntax-validated. NOT yet executed in prod (registry deploy, CI VM, DNS, ingress roll, first deploy, PR open) — gated on operator go-ahead per BL-083. CODE work; not double-counted against the parallel TESIEMS session.

TESIEMS 5hr: Built out TESIEMS Dev (customer Splunk + DISA-STIG IaC) at ~/tesiems. Recon of the pueo-mclean estate; ClickUp list "TESIEMS Dev ↑" (700-CTO ▸ Projects & Infrastructure, 13 tasks); scaffolded a standalone flat-text-emailable Ansible repo (7-phase lockout-safe site.yml, group_vars contract, ADR-0001, MANIFEST, disk-config.md). Decisions: SSG/ComplianceAsCode STIG + oscap verify (note: no official DISA RHEL/Rocky-10 STIG yet — using SSG rl10 anticipatory profile); Vault + deploy-time-generated secrets; Splunk 9.x trial→drop-in license; air-gapped+mirror, Splunk hand-carried; collections mirror-first (not emailed). Implemented all 11 roles (5 hardening authored: bootstrap/disk_layout/stig/scap_scan/evidence; 6 Splunk via parallel workflow + integration review). Built the lean Rocky 10 golden-template build path (qcow2→VMDK staged+checksum-verified, OVF, cloud-init seed) + lab-only Terraform (vm-template module + dev-splunk 9-VM stack, terraform validate clean). Full ansible syntax-check green; terraform validate clean. Ran an adversarial multi-agent correctness review (18 findings) and fixed the high/medium ones in-tree (notably a HF [monitor:] vs [monitor://] typo that silently broke all syslog ingest, and pass4SymmKey /dev/null non-persistence that desynced the cluster on re-runs); documented 4 low-confidence items for live-test. Then (operator-authorized) used the mclean Ansible Vault vCenter creds to take it live: diagnosed + fixed the Rocky10 template build via serial console (lsilogic->pvscsi for the dracut/disk hang; open-vm-tools must be baked in so cloud-init's VMware guestinfo datasource can inject per-clone config; net.ifnames=0 for eth0). Built the template, provisioned all 9 dev VMs (guarded, plan-reviewed terraform), validated 9/9 up on static IPs. Fixed a real deliverable bug: vaulted secrets must live in group_vars/all/ (top-level vault.yml binds to a nonexistent group). Pushed the repo to github.com/chris-pueo/tesiems-iac (main). Live hardening loop (bootstrap->stig->scap) running on one indexer; full site.yml + Splunk + evidence next.

Technology_MGMT 0.5hr: Staged NetBird remote-access for new team member Tony Covert (tony.covert@pueo.com) — operator couldn't reach the internal dashboard, so created the user via the management API (POST /api/users, role=user, auto_groups=[team] only — deliberately NOT cto/mgmt-subnets/rp-mclean to avoid the router-pool mis-grouping from the 6/11 incident; verified routing_peers_count still 4). team group = same subnet reach as cto via team-to-mgmt + lab-internal-dns tunnel DNS. Also created his Obsidian CouchDB editor account (tony.covert, km-editor on 10.48.9.28). Temp/CouchDB passwords handed to operator in-session for out-of-band delivery; CA-root trust + E2E passphrase remain operator-delivered. CHG-2026-0045. Flagged: CHG-2026-0046 frames Tony as an app dev meant to "test without touching infra" — surfaced the CTO-equivalent access scope for operator review.
Technology_MGMT 2hr: Built read-only Ansible audit role + audit.yml (brownfield Rocky10 state capture: OS/FIPS/SELinux/sshd -T/users/sudo/pkgs/dnf-history/net/firewalld/storage/systemd/recent-/etc + opportunistic non-remediating SCAP CAT tally + Splunk detect/version/conf slurp/cluster-status); consolidated reports/audit/AUDIT-<ts>.md from hostvars; templates rendered + verified, ansible-playbook --syntax-check passes. ClickUp #10.

TESIEMS 3hr: Brownfield pivot for the TESIEMS customer (their gov team hand-jammed the Rocky10 hosts with no config mgmt instead of resetting to baseline). Built + LIVE-VALIDATED the auto-changelog engine: changelog/descriptors.yml (29 human descriptors authored here) + callback_plugins/tesiems_changelog.py renders a coherent reports/CHANGELOG-<ts>.md in-env with NO LLM — records every CHANGED task tagged chg_<id> with the staged descriptor + real before/after diff; wired into ansible.cfg, bootstrap change-tasks tagged. Built the yaml/txt-only TRANSPORT (transport/pack.sh -> base64 every non-yaml/txt file to .txt that content-sniffs as text/plain per the gateway's magic-byte check + sha256/mode index; transport/unpack.txt restores exactly via `bash unpack.txt`) — round-trip verified 105 files, 0 mismatches incl. the binary .docx. ADR-0002 + brownfield-audit-and-changelog runbook. Fixed bootstrap lockout guard (-o IdentitiesOnly=yes — STIG MaxAuthTries was false-failing the probe). Live test on linsdevidx001: drifted chrony.conf -> converge fixed it + auto-changelog wrote the ntp_config entry w/ diff -> idempotent re-run = "no tracked changes". Found (true positive) idx001 key-only login broken while password auth still on; guard correctly refused. Pushed to github.com/chris-pueo/tesiems-iac main (8a920c7). ClickUp #11/12/13.
