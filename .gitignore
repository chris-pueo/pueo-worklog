# OS / editor junk
Thumbs.db
Desktop.ini
.DS_Store
*.swp

# sync lock (created by bin/sync-push.sh)
.sync.lock

# per-file narrative append lock (created by bin/claude-worklog-hook.sh SessionEnd backstop)
narrative/*.lock
